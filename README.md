# Template

This is a fully empty project.

The following packages have been installed though: `cucumber`, `prettier`, `tsx`, `typescript`.

The project currently has two "scripts":

| Script | Description |
|--------|-------------|
| test   | Run cucumber tests |
| prettier | Run prettier |


